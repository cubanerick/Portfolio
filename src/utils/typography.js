import Typography from 'typography'
import altonTheme from 'typography-theme-alton'
altonTheme.headerFontFamily = ['Open Sans']

const typography = new Typography(altonTheme)

export default typography